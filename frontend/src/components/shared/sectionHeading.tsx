const SectionHeading = ({ title }: { title: string }) => {
  return (
    <div className="">
      <h2 className="text-[28px] font-nunito font-normal">{title}</h2>
    </div>
  );
};

export default SectionHeading;
